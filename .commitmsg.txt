feat(logo): 替换为新版 Ore UI 应用图标并提升界面文字对比度

LOGO 替换:
- 源图为 1254x1254 正方形图标（Ore UI 风格蓝色圆角方块 + 清单 + 铅笔）
- build_logo.py 重写为按纯白背景做颜色距离抠图：阈值区间内按比例给 alpha,
  保留原图抗锯齿边缘, 并清理低 alpha 噪点
- 新增 clear_corner_haze: 清除小尺寸 Lanczos 降采样在圆角外侧留下的极淡残影
- 16/32px 使用主体占比更高的小尺寸专用画布, 提升极小尺寸下的辨识度
- 新增 logo_512.png 供文档使用; app.ico 内嵌 6 个尺寸

界面可读性:
- 顶栏 LOGO 改用 icon_64.png, 避免为 34px 显示加载 1116px 大图
- 空状态图标由内联 SVG 换为 icon_48.png
- 修复亮色主题下按钮文字对比度不足: 幽灵按钮文字色从 --text-soft 提升到
  --text-main, 实测对比度由 3.29 提升至 7.22 (达到 WCAG AA)
- 同步提高两套主题的 --text-soft 与 --text-dim, 改善小字号元信息可读性
- 卡片删除按钮由实心红改为克制版 btn-danger-ghost, 默认低调、悬停转红,
  消除列表中大面积红色造成的视觉噪音
- 已完成任务改为只降低内容区透明度, 保持操作按钮清晰可读

验证:
- startup_check.py 移入仓库根目录, 新增 CSS 变量完整性检查
  (自动发现 var() 引用了未定义的变量), 21 项断言全部通过
- selftest.py 51 项断言全部通过
- exe 重新打包并验证 6 个图标尺寸全部正确内嵌
- 补充 README 的图标生成与自测说明
